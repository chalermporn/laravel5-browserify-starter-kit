var $ = window.jQuery = require('jquery'),
    bootstrap = require('bootstrap-sass'),
    ujs       = require('jquery-ujs'),
    editor    = require('./_editor'),
    uplder    = require('./_uploader');

$(function(){
    //
});
